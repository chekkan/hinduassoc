.container
    table.table.table-hover.table-striped.table-condensed
        thead
            tr
                th Title
                th Event Date
        tbody
            tr(ng-repeat="event in events")
                td {{ event.title }}
                td {{ event.eventDate | date }}