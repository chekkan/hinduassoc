.container
    .page-header
        h1 Users
    a.btn.btn-default(href="/users/new") New User
    ul.list-group
    li.list-group-item(ng-repeat="user in users")
        a(href="/profile/{{user._id}}") {{user.firstName + " " + user.lastName}}